<%= form_for(@sub_list) do |f| %>
  <% if @sub_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_list.errors.count, "error") %> prohibited this sub_list from being saved:</h2>

      <ul>
      <% @sub_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
